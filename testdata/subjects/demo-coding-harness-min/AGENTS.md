# Demo: minimal coding harness

This fixture is a **public, in-repo** stand-in for a subject workspace.
It exists so Harness-dev can test the subject-gate protocol without private remotes.

Trusted suite entry: `bash scripts/harness/test-harness.sh`
